// PATH: build.gradle (à la racine)
plugins {
  // ✅ déclare la version du plugin Android au niveau racine
  id 'com.android.application' version '8.6.1' apply false
}

tasks.register("clean", Delete) {
  delete(rootProject.buildDir)
}
